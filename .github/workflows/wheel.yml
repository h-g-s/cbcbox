name: cibuildwheel

on:
  workflow_dispatch:
    inputs:
      publish:
        description: 'Publish wheels to PyPI: (testpypi/pypi/none)'
        required: false
        type: choice
        options:
          - testpypi
          - pypi
          - none
        default: none
          

jobs:
  build_wheels:
    name: Build wheels on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - os: ubuntu-latest
            arch: x86_64
          - os: ubuntu-24.04-arm
            arch: aarch64
          - os: macos-15
            arch: arm64
          - os: macos-15-intel
            arch: x86_64
          - os: windows-latest
            arch: AMD64

    steps:
      - uses: actions/checkout@v4

      - name: Set up QEMU (for Linux aarch64 emulation)
        if: matrix.os == 'ubuntu-latest' && matrix.arch == 'aarch64'
        uses: docker/setup-qemu-action@v3
        with:
          platforms: arm64

      - name: Build wheels
        uses: pypa/cibuildwheel@v3.3.1
        env:
          CIBW_BUILD: "cp313-*"
          CIBW_SKIP: "*musllinux*"
          CIBW_ARCHS: "${{ matrix.arch }}"
          CIBW_MANYLINUX_X86_64_IMAGE: "quay.io/pypa/manylinux2014_x86_64"
          CIBW_MANYLINUX_AARCH64_IMAGE: "quay.io/pypa/manylinux2014_aarch64"
          # gfortran: needed to build OpenBLAS with full LAPACK support.
          # patchelf: used by bundle_dynamic_deps to set $ORIGIN rpaths and
          #           read ELF sonames of bundled libs (libgfortran, etc.).
          CIBW_BEFORE_ALL_LINUX: >
            yum install -y gcc-gfortran &&
            pip install patchelf
          # gfortran ships with the pre-installed Xcode / GCC on macOS runners.
          # Install it explicitly only if somehow absent.
          CIBW_BEFORE_ALL_MACOS: >
            which gfortran || brew install gcc
          # MSYS2 is pre-installed on windows-latest. Install the MinGW64
          # toolchain (GCC, gfortran, cmake, make) and MSYS2 autotools.
          # 'make' (no prefix) is the MSYS2 /usr/bin/make needed by autotools;
          # 'mingw-w64-x86_64-make' only provides mingw32-make and is not used.
          CIBW_BEFORE_ALL_WINDOWS: >
            C:\msys64\usr\bin\bash.exe -lc "pacman --noconfirm -S --needed
            mingw-w64-x86_64-gcc mingw-w64-x86_64-gcc-fortran
            make
            autoconf automake libtool pkg-config"
          CIBW_REPAIR_WHEEL_COMMAND_LINUX: ""
          CIBW_REPAIR_WHEEL_COMMAND_MACOS: ""
          # DLL bundling is done by setup.py; skip delocate/delvewheel.
          CIBW_REPAIR_WHEEL_COMMAND_WINDOWS: ""
          CIBW_TEST_REQUIRES: "pytest"
          CIBW_TEST_COMMAND: "pytest {package}/tests/ -v"
        with:
          package-dir: .
          output-dir: wheelhouse
          config-file: "{package}/pyproject.toml"

      - uses: actions/upload-artifact@v4
        with:
          name: cibw-wheels-${{ runner.os }}-${{ runner.arch }}
          path: ./wheelhouse/*.whl

      - name: Upload performance report
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: perf-report-${{ matrix.os }}-${{ matrix.arch }}
          path: |
            tests/perf_report.md
            tests/perf_report.json

  combine_reports:
    name: Combine performance reports
    needs: build_wheels
    runs-on: ubuntu-latest
    if: always()

    steps:
      - uses: actions/checkout@v4

      - name: Download all performance reports
        uses: actions/download-artifact@v4
        with:
          pattern: perf-report-*
          path: perf_reports/

      - name: Generate combined report
        run: python3 .github/scripts/combine_perf_reports.py perf_reports perf_report_combined.md

      - name: Upload combined report
        uses: actions/upload-artifact@v4
        with:
          name: perf-report-combined
          path: perf_report_combined.md

  publish-to-testpypi:
    name: Publish Python wheels to TestPyPI
    needs:
    - build_wheels
    runs-on: ubuntu-latest
    if: github.event.inputs.publish == 'testpypi'

    environment:
      name: testpypi
      url: https://test.pypi.org/p/cbcbox

    permissions:
      id-token: write

    steps:
    - name: Download all the dists
      uses: actions/download-artifact@v4
      with:
        pattern: cibw-wheels-*
        merge-multiple: true
        path: dist/
    - name: List all the dists
      run: ls -l dist/
    - name: Publish distribution to TestPyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        repository-url: https://test.pypi.org/legacy/

  publish-to-pypi:
    name: Publish Python wheels to PyPI
    needs:
    - build_wheels
    runs-on: ubuntu-latest
    if: github.event.inputs.publish == 'pypi'

    environment:
      name: pypi
      url: https://pypi.org/project/cbcbox

    permissions:
      id-token: write

    steps:
    - name: Download all the dists
      uses: actions/download-artifact@v4
      with:
        pattern: cibw-wheels-*
        merge-multiple: true
        path: dist/
    - name: List all the dists
      run: ls -l dist/
    - name: Publish distribution to PyPI
      uses: pypa/gh-action-pypi-publish@release/v1
      with:
        repository-url: https://upload.pypi.org/legacy/
